# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/

# Environment variables (local testing)
.env
.env.local

# Claude Code per-developer local state
.claude/settings.local.json

# Codex CLI per-developer local state (the codex/ tree is independent of plugins/)
.codex/
codex/config.toml

# Phase A generator output (derived, not source)
dist/

# Local scratch: reports, analyses, brainstorm reflections (per global CLAUDE.md)
claudedocs/

# Playwright MCP session output (traces, downloads, session state)
.playwright-mcp/
